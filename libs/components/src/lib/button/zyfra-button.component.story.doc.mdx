import { ArgsTable, Subtitle } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import { storiesOf } from '@storybook/angular';
import { ZyfraButtonComponent } from 'libs/components/src/lib/button/index';

## Button

<Subtitle>Button is an extension to standard button element with icons and theming.</Subtitle>

Import module:

```typescript
import { ZyfraButtonModule } from '@digital-plant/zyfra-components';

@NgModule({
  imports: [ZyfraButtonModule],
})
export class YourModule {}
```

To component:

```html
<zyfra-button label="example button" type="submit" (click)="onClickButton($event)"></zyfra-button>
```

We use our standard css classes for styling :

```
.p-button {
  &-mini
  &-default
  &-success
  &-secondary
  &-info
  &-danger
  &-warning
}
```

## ArgsTable

<ArgsTable of={ZyfraButtonComponent} />
