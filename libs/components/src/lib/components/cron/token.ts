import { InjectionToken } from '@angular/core';

export const PrizmCronUiTypeDetectorToken = new InjectionToken(
  'prizm UI cron type detector'
);

export const PrizmCronUiItemsToken = new InjectionToken(
  'prizm UI cron items'
);

export const PrizmCronUiListToken = new InjectionToken(
  'prizm UI cron list with days, weeks, months, hours, items list',
);
