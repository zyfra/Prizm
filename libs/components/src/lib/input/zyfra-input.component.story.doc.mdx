import { ArgsTable, Subtitle } from '@storybook/addon-docs';
import { ZyfraInputComponent } from './zyfra-input.component.ts';
import { moduleMetadata } from '@storybook/angular';
import { storiesOf } from '@storybook/angular';

## Zyfra - Input

<Subtitle>InputText renders a text field to enter data</Subtitle>

Import module:

```typescript
import { ZyfraInputModule } from '@digital-plant/zyfra-components';

@NgModule({
  imports: [ZyfraInputModule],
})
export class YourModule {}
```

To component:

```html
<zyfra-input
  [id]="id"
  [type]="type"
  [(ngModel)]="ngModel"
  [disabled]="disabled"
  [placeholder]="placeholder"
  [tooltip]="tooltip"
  [tooltipPosition]="tooltipPosition"
  [label]="label"
></zyfra-input>
```

```typescript
public ngModel = 'some string';
```

#### Custom content:
```html

 <ng-content select="[right || left]"></ng-content>

```

## ArgsTable

<ArgsTable of={ZyfraInputComponent} />