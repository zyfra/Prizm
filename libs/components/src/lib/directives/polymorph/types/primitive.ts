import type { SafeValue } from '@angular/platform-browser';

/**
 * Primitive types used as content by {@link PolymorphOutletDirective}
 */
export type PolymorphPrimitive = SafeValue | number | string | null | undefined;
