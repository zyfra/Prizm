import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  ColorPalette,
  ColorItem,
  Preview,
  Typeset,
  Title,
  Subtitle,
  Description,
} from '@storybook/addon-docs';
import ZyfraProgressBarComponent from './zyfra-progress-bar.component';
import { moduleMetadata } from '@storybook/angular';
import { storiesOf } from '@storybook/angular';

## Zyfra - ProgressBar

<Subtitle>ProgressBar is a process status indicator.</Subtitle>

To module:

```typescript
import { ZyfraProgressBarModule } from '@digital-plant/zyfra-components';

@NgModule({
  imports: [ZyfraProgressBarModule],
})
export class YourModule {}
```

To component:

```html
<zyfra-progress-bar [value]="value"></zyfra-progress-bar>
```

```typescript
public messages value: number = 0;
```

Other examples:

```html
<zyfra-progress-bar [mode]="indeterminate"></zyfra-progress-bar>
```

```html
<zyfra-progress-bar [value]="50" [showValue]="false"></zyfra-progress-bar>
```

```html
<zyfra-progress-bar [value]="50" color="success" size="mini"></zyfra-progress-bar>
```

## ArgsTable

<ArgsTable of={ZyfraProgressBarComponent} />
