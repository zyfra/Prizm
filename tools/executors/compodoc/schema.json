{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "cli": "nx",
  "properties": {
    "tsConfigPath": {
      "type": "string",
      "description": "Path to tsconfig"
    },
    "distPath": {
      "type": "string",
      "description": "Path to dist folder"
    }
  }
}
