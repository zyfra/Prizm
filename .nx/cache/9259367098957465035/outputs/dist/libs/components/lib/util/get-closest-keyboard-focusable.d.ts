/**
 * Finds the closest element that can be focused with a keyboard or mouse
 */
export declare function prizmGetClosestFocusable(initial: HTMLElement, prev: boolean | undefined, root: Node, keyboard?: boolean): HTMLElement | null;
